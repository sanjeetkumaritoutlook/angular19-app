import { TitleCaseExceptPipe } from './title-case-except.pipe';

describe('TitleCaseExceptPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleCaseExceptPipe();
    expect(pipe).toBeTruthy();
  });
});
